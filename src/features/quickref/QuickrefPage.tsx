import type { JSX } from "react";

import { PagePlaceholder } from "@/components/PagePlaceholder";

export function QuickrefPage(): JSX.Element {
  return (
    <PagePlaceholder
      title="クイック表"
      description="症状から処置を引く表を表示します（Step 06）。"
    />
  );
}
