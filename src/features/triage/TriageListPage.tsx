import type { JSX } from "react";

import { PagePlaceholder } from "@/components/PagePlaceholder";

export function TriageListPage(): JSX.Element {
  return (
    <PagePlaceholder
      title="トリアージ"
      description="トリアージフローの一覧を表示します（Step 07）。"
    />
  );
}
