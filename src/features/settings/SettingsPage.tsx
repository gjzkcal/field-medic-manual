import type { JSX } from "react";

import { PagePlaceholder } from "@/components/PagePlaceholder";

export function SettingsPage(): JSX.Element {
  return <PagePlaceholder title="設定" description="アプリの設定を表示します（Step 08 以降）。" />;
}
