import type { JSX } from "react";

import { PagePlaceholder } from "@/components/PagePlaceholder";

export function LibraryPage(): JSX.Element {
  return (
    <PagePlaceholder
      title="ライブラリ"
      description="取り込んだマニュアルの一覧を表示します（Step 04）。"
    />
  );
}
